node_modules
.env
.env.local
.env.*.local
dist
.next
.turbo
coverage
*.log
.DS_Store

# Mobile / Expo
.expo/
apps/mobile/ios/
apps/mobile/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Firebase server-side secrets (admin SDK json içeriği hassas — çıkmasın)
.firebase-admin*.json

# Not: apps/mobile/GoogleService-Info.plist ve google-services.json commit ediliyor.
# Bunlar Firebase'in "public client config" dosyaları — API key + project ID içerir,
# güvenlik Firebase Security Rules ile sağlanır (Firebase resmi dokümantasyonu).
# EAS build'e girmesi için repo'ya girmesi gerek.

# SSH keys
*.pem
emafly-new-server*

# Build artifacts
apps/mobile/ios/build/
apps/mobile/android/build/

